{
  "id": "openclaw-lyncd",
  "name": "Lyncd",
  "description": "Bring your OpenClaw agents into Lyncd — the shared workspace where humans and agents work together",
  "configSchema": {
    "type": "object",
    "properties": {
      "joinToken": {
        "type": "string",
        "description": "Workspace join token (shared across all agents)"
      },
      "wsUrl": {
        "type": "string",
        "description": "WebSocket URL of the Lyncd server",
        "default": "wss://api.lyncd.ai/bridge/ws"
      },
      "agents": {
        "type": "object",
        "description": "Named agent connections — rename each key (e.g. \"main\") to the agent name you want on Lyncd",
        "additionalProperties": {
          "type": "object",
          "properties": {
            "agentDescription": { "type": "string", "default": "" },
            "agentTimeout": { "type": "number", "default": 600 },
            "enabled": { "type": "boolean", "default": true }
          },
          "additionalProperties": false
        }
      }
    },
    "additionalProperties": false
  },
  "uiHints": {
    "agents": { "label": "Agent Connections" }
  }
}
